TUN 是 网络隧道（Tunnel）的一种实现方式，常用于通过虚拟网络接口将数据包从一个网络传输到另一个网络。它通常用于 VPN（虚拟专用网络）中来实现端到端加密和隐私保护。具体来说，TUN 是一种用于操作 IP 层的虚拟网络接口，它将数据包从一个虚拟网络传递到另一个虚拟网络。


操作层次：TUN 设备操作的是 IP 层。它处理的是原始的 IP 数据包，而不像 TAP（另一个常见的隧道接口）那样处理以太网帧。
