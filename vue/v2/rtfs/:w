* index.html
#+begin_src html
<div id="app">
  {{ message }}
</div>


<script src="https://cdn.jsdelivr.net/npm/vue@2/dist/vue.js"></script>  
<script>
var app = new Vue({
  el: '#app',
  data: {
    message: 'Hello Vue!'
  }
})
#+end_src

new -> _init -> $mount

* web-compiler $mount
// el el is app HTMLElement
this.el = query(el);
// this real call compiler_to_render("{{message}}")
this.$options.render = compiler_to_render(getOuterHTML(el))
// call core $mount
this.$mount(el, hydrating = undefined)

* core $mount 
// lifecycle.js
call mountComponent()
